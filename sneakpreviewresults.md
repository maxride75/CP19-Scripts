***Script resulting from Sneak preview***


updating was already done, so either just do it manually, or have apt do it -> preference is to have mint do it for auto update
Have the script say what it's doing
Pam files created, but were not activated -> add line to say to manually do it
I had to manually uninstall pkgs because mint store didn't show installed pkgs -> learn pkg names to apt remove
**USERS DID NOT WORK AT ALL!**
Add y/n prompt for user management, bc I clicked enter and scripts interrupted -> done, undo nested case
script interrupt did not do login defs, so not tested -> seems like it would work tho
add the rest of the security configs
add service finding and autonomous securing
Add choose module at start of script
